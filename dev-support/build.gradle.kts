plugins {
    id("dev.root")
    kotlin("multiplatform") version "1.9.20-RC" apply false
}

buildscript {
    repositories {
        mavenCentral()
        mavenLocal()
    }
    dependencies {
    }
}
